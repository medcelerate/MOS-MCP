# Binaries and build output
/mos-mcp
/dist/
/build/
*.exe
*.mcpb

# Downloaded build tools (Tailwind CLI)
/.tools/

# Local config (do not commit real deployment config)
/config.yaml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
